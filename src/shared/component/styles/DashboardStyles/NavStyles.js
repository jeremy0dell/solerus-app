const logo = {
  height: 35,
}

const searchBar = {
  width: 300,
  height: 40,
}

const account = {
  textTransform: 'uppercase',
  borderBottom: '5px solid #32CD32',
  fontWeight: 'bold',
}

export default { logo, searchBar, account }
