const authButtons = {
  position: 'absolute',
  top: '5%',
  right: '3.5%',
}

const authButton = {
  height: 50,
}

const launchText = {
  textTransform: 'capitalize',
  fontFamily: 'Inter UI',
  fontSize: 14,
  lineHeight: '28px',
  color: '#CFCFCF',
}

export default { authButtons, authButton, launchText }
